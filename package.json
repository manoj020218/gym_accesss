{
  "name": "edge-gym-access-control",
  "private": true,
  "version": "1.0.0",
  "description": "EDGE Gym Access Control — Local-first ERP + Multi-branch + Member App",
  "engines": { "node": ">=18.0.0", "pnpm": ">=9.0.0" },
  "scripts": {
    "build":   "pnpm -r build",
    "dev":     "pnpm -r --parallel dev",
    "test":    "pnpm -r test",
    "lint":    "pnpm -r lint",
    "clean":   "pnpm -r exec -- rm -rf dist node_modules"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
